// npx playwright test tests/assignments/assignment13/assignment13.spec.js --trace on --headed

import { expect, test } from '@playwright/test';
import { todos } from './todos.json';

test.describe('Assignment 13.', () => {
/*
    // Import the URL from the environment variables.
    ...

    test('Final asseignment', async ({ page }) => {
        // Navigate to the URL.
        ...

        // Loop through the todos and add them to the list.
        for (const todo of todos) {
            ...
        }

        // Change the todo "Do laundry" into "Do laundry and iron".
        // Codegen is not showing the correct flow for this action.
        // use: dblclick, fill, press
        ...

        // Verify that the todo "Do laundry and iron" is displayed.
        ...

    });
*/
});